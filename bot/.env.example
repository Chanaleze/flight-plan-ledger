# Copy to bot/.env (never commit .env — it holds the app's private key).
APP_ID=123456
PRIVATE_KEY_PATH=./flight-plan-ledger-helper.private-key.pem
WEBHOOK_SECRET=replace-me-with-a-random-string
PORT=3000
