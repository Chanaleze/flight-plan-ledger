# Dependencies
node_modules/
venv/
.venv/
__pycache__/
*.pyc
*.pyo
.Python
*.egg-info/
dist/
build/
.eggs/

# IDE / Editor
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Environment & secrets
.env
.env.*
!.env.example
*.pem
*.key
secrets/
keys/
# Account/app recovery codes (2FA, hosts, etc.) - never commit these
*recovery-cod*

# Logs & runtime
logs/
*.log
tmp/
temp/
.cache/

# Test & coverage
coverage/
.coverage
.coverage.*
.nyc_output/
htmlcov/
.pytest_cache/
.tox/
.mypy_cache/

# Docker / local data
data/
volumes/
*.db
*.sqlite

# Terraform
.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl

# OS
Thumbs.db

# Prototype runtime data
data/
*.jsonl

# Local recovery exports (operational artefacts, keep out of git)
recovery.json
recovery-*.json
data/recovery-*.json

# Local Node tooling (opencode-ai dev install, keep out of git)
# Scoped to root so bot/package.json (the Probot app) stays committable.
/package.json
/package-lock.json
