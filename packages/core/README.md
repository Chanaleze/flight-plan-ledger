# packages/core — FUTURE

Reserved for a future multi-language core package (shared ledger primitives).
The working Python prototype lives in `src/flight_plan_ledger/`. Do not start
here until a second language port is actually scoped.
