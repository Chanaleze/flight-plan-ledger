# services/ingestor — FUTURE

Reserved skeleton for a future Ingestor service (receive, normalise, validate plans).
Current equivalent: `normalize_flight_plan()` in `src/flight_plan_ledger/models/canonical.py`.
